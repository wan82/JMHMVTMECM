# Python dependencies for codec-comparison-pilot
# Install with:
#   python3 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt

numpy>=1.26
pandas>=2.2
scipy>=1.12
matplotlib>=3.8
pyyaml>=6.0
tqdm>=4.66
bjontegaard>=1.3.0
tabulate>=0.9      # required by pandas.DataFrame.to_markdown in build_report.py
